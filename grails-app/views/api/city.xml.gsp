<?xml version="1.0" encoding="UTF-8"?>
<Response>
  <Status>${status}</Status>
  <CountryCode>${geoCityInstance.country.countryCode}</CountryCode>
  <CountryName>${geoCityInstance.country.countryName}</CountryName>
  <RegionCode>${geoCityInstance.region}</RegionCode>
  <City>${geoCityInstance.city}</City>
  <PostalCode>${geoCityInstance.postalCode}</PostalCode>
  <Latitude>${geoCityInstance.latitude}</Latitude>
  <Longitude>${geoCityInstance.longitude}</Longitude>
  <Ip>${ip}</Ip>
</Response>
